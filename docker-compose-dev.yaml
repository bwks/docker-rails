version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.app
    command: sh -c "rails db:create && rails db:migrate && rails db:reset && rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    ports:
      - "3000:3000"
